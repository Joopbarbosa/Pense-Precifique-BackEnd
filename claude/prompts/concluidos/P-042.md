# P-042 — DTOs: ClienteRequest/Response

**Épico:** Épico 5 — Clientes
**Camada:** back
**Depende de:** P-041 (entidade Cliente)
**Modelo recomendado:** Haiku

---

## Prompt

Cole este prompt no Claude Code:

---

Crie os DTOs de Cliente em `src/main/java/com/penseprecifique/api/dto/`.

**Leia o CLAUDE.md antes de começar.**

### 1. `ClienteRequest.java`

```java
public class ClienteRequest {

    @NotBlank(message = "O nome do cliente é obrigatório")
    private String nome;

    private String email;

    private String whatsapp;

    private String endereco;

    private String observacoes;
}
```

### 2. `ClienteResponse.java`

```java
public class ClienteResponse {
    private UUID id;
    private String nome;
    private String email;
    private String whatsapp;
    private String endereco;
    private String observacoes;
    private boolean ativa;
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
}
```

Compile ao final:
```bash
./mvnw compile -q
```

---

## Checklist de validação

- [ ] `ClienteRequest` com `@NotBlank` em nome
- [ ] `ClienteResponse` com campo `ativa` (não `ativo`)
- [ ] `./mvnw compile -q` sem erros

## Commit

```bash
git add .
git commit -m "feat(clientes): adiciona DTOs ClienteRequest e ClienteResponse"
```

## Ao concluir

Informe "P-042 concluído" para receber o P-043.
