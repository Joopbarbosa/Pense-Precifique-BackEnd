# P-016 — DTOs: InsumoRequest/Response, MovimentacaoResponse

**Épico:** Épico 3 — Insumos
**Camada:** back
**Depende de:** P-015 (entidades Insumo e MovimentacaoInsumo)
**Modelo recomendado:** Haiku

---

## Contexto para o Claude Code

Esta tarefa cria os DTOs de entrada e saída para o fluxo de Insumos. Nenhuma lógica de negócio aqui — apenas estrutura de dados e validações de campo.

**Regras relevantes:**
- RN-034: listagens paginadas retornam 20 itens por vez
- RN-035: observação obrigatória mín. 50 chars para baixa manual (validação no DTO de baixa)
- RN-036: compra sempre via lote — não há DTO de compra individual

---

## Prompt

Cole este prompt no Claude Code:

---

Crie os DTOs de Insumo e MovimentacaoInsumo em `src/main/java/com/penseprecifique/api/dto/`.

**Leia o CLAUDE.md antes de começar.**

### 1. `InsumoRequest.java`

```java
public class InsumoRequest {
    @NotBlank(message = "O nome do insumo é obrigatório")
    private String nome;

    private String marca; // opcional

    @NotBlank(message = "A unidade de medida é obrigatória")
    private String unidadeMedida;

    @DecimalMin(value = "0", message = "O estoque atual não pode ser negativo")
    private BigDecimal estoqueAtual; // opcional no cadastro, default 0

    @DecimalMin(value = "0", message = "O estoque mínimo não pode ser negativo")
    private BigDecimal estoqueMinimo; // opcional
}
```

### 2. `InsumoResponse.java`

```java
public class InsumoResponse {
    private UUID id;
    private String nome;
    private String marca;
    private String unidadeMedida;
    private BigDecimal custoUnitario;
    private BigDecimal estoqueAtual;
    private BigDecimal estoqueMinimo;
    private boolean ativo;
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
}
```

### 3. `BaixaManualInsumoRequest.java`

```java
public class BaixaManualInsumoRequest {
    @NotNull(message = "A quantidade é obrigatória")
    @DecimalMin(value = "0.001", message = "A quantidade deve ser maior que zero")
    private BigDecimal quantidade;

    @NotNull(message = "O motivo é obrigatório")
    private MotivoMovimentacaoInsumo motivo; // apenas: BAIXA_MANUAL

    @NotBlank(message = "A observação é obrigatória")
    @Size(min = 50, message = "A observação deve ter no mínimo 50 caracteres")
    private String observacao;
}
```

### 4. `MovimentacaoInsumoResponse.java`

```java
public class MovimentacaoInsumoResponse {
    private UUID id;
    private TipoMovimentacaoInsumo tipo;
    private MotivoMovimentacaoInsumo motivo;
    private BigDecimal quantidade;
    private String observacao;
    private UUID referenciaId;
    private ReferenciaMovimentacaoTipo referenciaTipo;
    private boolean estornada;
    private LocalDateTime createdAt;
}
```

### 5. `PageResponse.java` (genérico — apenas se ainda não existir no projeto)

```java
public class PageResponse<T> {
    private List<T> content;
    private int page;
    private int size;
    private long totalElements;
    private boolean hasNext;
}
```

> Verifique se já existe um wrapper de paginação genérico no projeto antes de criar — se existir, reutilize.

### Convenções:
- Use Lombok (`@Getter`, `@Setter`, `@Builder`, `@NoArgsConstructor`, `@AllArgsConstructor`) se já estiver no projeto
- Imports dos enums do pacote `com.penseprecifique.api.domain.enums`

Compile ao final:
```bash
./mvnw compile -q
```

---

## Checklist de validação

- [ ] `InsumoRequest` com `@NotBlank` em nome e unidadeMedida
- [ ] `BaixaManualInsumoRequest` com `@Size(min = 50)` em observacao (RN-035)
- [ ] `MovimentacaoInsumoResponse` com campo `estornada`
- [ ] `PageResponse<T>` genérico criado (ou reutilizado se já existia)
- [ ] `./mvnw compile -q` sem erros

## Commit

```bash
git add .
git commit -m "feat(insumos): adiciona DTOs InsumoRequest/Response e MovimentacaoInsumoResponse"
```

## Ao concluir

Informe "P-016 concluído" para receber o P-017.
