<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.penseprecifique</groupId>
    <artifactId>pense-precifique-api</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>pense-precifique-api</name>
    <description>Back-end da aplicação Pense e Precifique</description>

    <properties>
        <java.version>21</java.version>
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>2.3.0</springdoc.version>
        <!-- Override: 1.18.38 suporta Java 25 -->
        <lombok.version>1.18.38</lombok.version>
    </properties>

    <!--
        OP #419 — spring-boot-starter-parent:3.3.5 gerencia versões com CVEs HIGH/CRITICAL
        conhecidos (achado da skill seguranca-resiliencia, OSV-Scanner). A branch 3.3.x do
        Spring Boot atingiu EOL (30/06/2025) — não haverá patch 3.3.x futuro que resolva isso,
        forçar aqui é a única via enquanto o projeto não migra para uma branch ainda mantida
        (3.4/3.5). Overrides confirmados compatíveis com Servlet 6.0/Jakarta EE 10 (linha
        10.1.x do Tomcat) e semver do Jackson (2.15.4 -> 2.18.10, mesma major 2.x).
    -->
    <dependencyManagement>
        <dependencies>
            <!-- Tomcat embarcado: 10.1.31 -> 10.1.55+ corrige GHSA-5m62-pw8w-7w9f (CRITICAL)
                 e demais CVEs HIGH/CRITICAL da rodada. Os 3 artefatos embed precisam da
                 mesma versão. -->
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>10.1.59</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-el</artifactId>
                <version>10.1.59</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>10.1.59</version>
            </dependency>
            <!-- jackson-core/-databind/-annotations -> 2.18.10 (última patch da linha 2.18,
                 corrige CVE-2026-59888/59889/68497 HIGH). Os 3 módulos Jackson precisam da
                 mesma versão entre si. -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>2.18.10</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>2.18.10</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>2.18.10</version>
            </dependency>
            <!-- json-smart HIGH (DoS, CVE-2023-1370/CVE-2024-57699) -> 2.5.2 corrige. -->
            <dependency>
                <groupId>net.minidev</groupId>
                <artifactId>json-smart</artifactId>
                <version>2.5.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- PostgreSQL -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Flyway -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!--
            Cloudflare R2 (compatível S3) — upload de foto do Item de Catálogo, DT-NOVA-4.
            R2StorageClient constrói o S3Client explicitamente com UrlConnectionHttpClient
            (java.net.HttpURLConnection, zero dependência de terceiros) — os clientes HTTP que o
            módulo "s3" traz por padrão nunca são usados em runtime, mas continuam entrando no
            classpath (e no scanner de CVE) se não forem excluídos aqui.
            OpenProject #524 (achado OSV-Scanner, gate seguranca-resiliencia V0.13.0): a exclusão
            original mirava o artifactId "apache-client" — nome do cliente HTTP síncrono padrão em
            versões antigas da AWS SDK v2 — mas na 2.55.1 o módulo "s3" já não depende mais dele;
            passou a depender de "apache5-client" (baseado em httpclient5, daí o
            NoClassDefFoundError em TlsSocketStrategy que motivou excluir algo aqui, na decisão
            original) e "netty-nio-client" (cliente assíncrono). A exclusão antiga era um no-op —
            nenhum dos dois nunca foi de fato excluído, o que carregava Netty e httpclient5/httpcore5
            (com os CVEs reportados) no classpath sem necessidade. Confirmado via
            `mvn dependency:tree -Dincludes=io.netty,org.apache.httpcomponents.core5`: antes desta
            correção, ambos entravam só através de "s3"; depois, o `dependency:tree` não retorna
            nenhuma ocorrência.
        -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>2.55.1</version>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache5-client</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
            <version>2.55.1</version>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- MapStruct -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>

        <!-- Springdoc OpenAPI (Swagger) -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- WireMock — mocka o HTTP do microsserviço pense-precifique-pdf em PdfMicroservicoClientIT.
             "-standalone" (não "wiremock" puro) de propósito: sombreia Jetty/Apache HttpClient5
             internamente sob o pacote "wiremock.*" — o artefato "wiremock" comum exige que o
             consumidor traga a própria versão de Jetty/httpclient5, e as versões que ele pede
             (Jetty 12.0.14, httpclient5 5.5) colidem com as que spring-boot-dependencies gerencia
             (nenhuma delas é usada por este projeto fora do WireMock, então não há downgrade real
             a proteger — só um conflito de resolução de versão sem consumidor de verdade). -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>3.13.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Onda 3 (#136 relato) — includes padrão do Surefire (**/*Test.java etc.) nunca
                         pegavam os testes deste projeto, todos nomeados *IT.java (convenção de
                         integration test). "mvn test" sempre rodou 0 testes silenciosamente, sem essa
                         config; ninguém tinha notado porque a suíte completa nunca tinha sido rodada de
                         uma vez (sempre por classe, via -Dtest=NomeDaClasse). -->
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                        <include>**/*TestCase.java</include>
                        <include>**/*IT.java</include>
                    </includes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <!-- Lombok DEVE vir antes do MapStruct -->
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
